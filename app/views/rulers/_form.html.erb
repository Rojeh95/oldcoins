<%= form_with(model: ruler, local: true) do |form| %>
  <% if ruler.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ruler.errors.count, "error") %> prohibited this ruler from being saved:</h2>

      <ul>
      <% ruler.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label t("dynasties.rulers_name") %><br>
    <%= form.text_area :ru_name, id: :ruler_ru_name %>
  </div>

  <div class="field">
   
    <%= form.hidden_field :dynasty_id, id: :ruler_dynasty_id %>
  </div>
  <div class="field">
    <label for="file-input">
      <span class="glyphicon glyphicon-camera"></span>
    </label>
    <%= form.label t("dynasties.image") %>
    <%= form.file_field :ru_image %>
  </div>

  <div class="actions">
   <%= form.submit t("dynasties.create")%>
  </div>
<% end %>
