<p id="notice"><%= notice %></p>


<% @dynasties.each do |dynasty| %>
    <div class="col-sm-4">
        <div class="container"> 
        <%=link_to image_tag("#{dynasty.dy_image}".html_safe ,width: 350, height:350),dynasties_probe_path(dynasty)%>
       <h2><%= link_to dynasty.dy_name, dynasty %>   </h2>
        </div>
     
    </div>
    <% end %>

<br>
<br>






<!-- <h1>Dynasties</h1>

<table>
  <thead>
    <tr>
      <th>Dy name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @dynasties.each do |dynasty| %>
      <tr>
        <td><%= dynasty.dy_name %></td>
        <td><%= image_tag("#{dynasty.dy_image}".html_safe ,width: 200, height:150)%></td>
        <td><%= link_to 'Show', dynasty %></td>
        <td><%= link_to 'Edit', edit_dynasty_path(dynasty) %></td>
        <td><%= link_to 'Destroy', dynasty, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'New Dynasty', new_dynasty_path(@dynasty) %>
<br>
 -->
